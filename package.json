{
  "name": "typora-md-wysiwyg",
  "displayName": "Typora Markdown (WYSIWYG)",
  "description": "Typora-like same-tab live typesetting WYSIWYG markdown editor for VS Code, powered by Vditor. Renders and lets you edit markdown in place (no split preview) with theme switching.",
  "version": "0.1.0-beta.4",
  "publisher": "typoramd-local",
  "private": true,
  "license": "MIT",
  "author": {
    "name": "ZhaoRibo",
    "url": "https://github.com/ZhaoRibo"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ZhaoRibo/TyporaMD.git"
  },
  "bugs": {
    "url": "https://github.com/ZhaoRibo/TyporaMD/issues"
  },
  "homepage": "https://github.com/ZhaoRibo/TyporaMD#readme",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "markdown",
    "wysiwyg",
    "typora",
    "editor",
    "preview",
    "vditor"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onCustomEditor:typoraMd.editor",
    "onCommand:typoraMd.openWysiwyg",
    "onCommand:typoraMd.openAsText",
    "onCommand:typoraMd.setTheme",
    "onLanguage:markdown"
  ],
  "contributes": {
    "commands": [
      {
        "command": "typoraMd.openWysiwyg",
        "title": "Typora: Open Current File in WYSIWYG View",
        "category": "Typora"
      },
      {
        "command": "typoraMd.openAsText",
        "title": "Typora: Reopen as Markdown Source",
        "category": "Typora"
      },
      {
        "command": "typoraMd.setTheme",
        "title": "Typora: Switch Editor Theme",
        "category": "Typora"
      },
      {
        "command": "typoraMd.toggleToolbar",
        "title": "Typora: Toggle Formatting Toolbar",
        "category": "Typora"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "when": "resourceLangId == markdown",
          "command": "typoraMd.openWysiwyg",
          "group": "1_typora"
        },
        {
          "when": "resourceLangId == markdown",
          "command": "typoraMd.openAsText",
          "group": "1_typora"
        }
      ],
      "editor/title/context": [
        {
          "when": "resourceLangId == markdown",
          "command": "typoraMd.openWysiwyg",
          "group": "1_typora"
        },
        {
          "when": "resourceLangId == markdown",
          "command": "typoraMd.openAsText",
          "group": "1_typora"
        }
      ],
      "editor/context": [
        {
          "when": "resourceLangId == markdown && editorTextFocus",
          "command": "typoraMd.openWysiwyg",
          "group": "1_typora"
        }
      ]
    },
    "customEditors": [
      {
        "viewType": "typoraMd.editor",
        "displayName": "Typora Markdown (WYSIWYG)",
        "selector": [
          {
            "filenamePattern": "*.md"
          },
          {
            "filenamePattern": "*.markdown"
          },
          {
            "filenamePattern": "*.mdown"
          },
          {
            "filenamePattern": "*.mkd"
          }
        ],
        "priority": "option"
      }
    ],
    "configuration": {
      "title": "Typora Markdown (WYSIWYG)",
      "properties": {
        "typoraMd.autoOpen": {
          "type": "boolean",
          "default": true,
          "description": "Open markdown files in the same-tab Typora-style WYSIWYG editor automatically. Turn off to keep the normal text editor and open the WYSIWYG view manually.",
          "markdownDescription": "Open markdown files in the same-tab Typora-style WYSIWYG editor automatically. Turn off to keep the normal text editor and open the WYSIWYG view manually."
        },
        "typoraMd.theme": {
          "type": "string",
          "enum": [
            "auto",
            "light",
            "dark"
          ],
          "default": "auto",
          "description": "Editor theme. 'auto' follows the active VS Code color theme (light/dark).",
          "markdownDescription": "Editor theme. `auto` follows the active VS Code color theme (light/dark). Use the command **Typora: Switch Editor Theme** to change it quickly."
        },
        "typoraMd.mode": {
          "type": "string",
          "enum": [
            "wysiwyg",
            "ir"
          ],
          "default": "wysiwyg",
          "description": "Editing mode. 'wysiwyg' (RECOMMENDED, default): Markdown markers are hidden so you only see the rendered result - closest to Typora's default look. 'ir' (instant rendering): markers stay visible (dimmed) on every block.",
          "markdownDescription": "编辑模式（区别在于 **Markdown 标记是否显示**）：\n- `wysiwyg`（所见即所得，**推荐 / 默认**）：标记**完全隐藏**，只看得到渲染结果 —— **日常观感最接近 Typora**（Typora 也只在光标所在那一块临时显示标记）。输入先作为文本保留，标记写完整（如 `> `）才转成结构。\n- `ir`（即时渲染）：**所有块**的标记都**保留可见**（淡化显示，如 `**`、`#`、`>`），内容实时渲染。\n\n本插件当前**默认 `wysiwyg`**：实测 `ir` 存在写回问题（行首 `>` 立即变引用、空行不写回文件），暂不作默认。"
        },
        "typoraMd.codeTheme": {
          "type": "string",
          "enum": [
            "auto",
            "github",
            "github-dark"
          ],
          "default": "auto",
          "description": "Syntax-highlighting theme for code blocks. 'auto' picks github in light mode and github-dark in dark mode.",
          "markdownDescription": "Syntax-highlighting theme for code blocks. `auto` picks `github` in light mode and `github-dark` in dark mode."
        },
        "typoraMd.autoSave": {
          "type": "boolean",
          "default": false,
          "description": "Save the markdown file automatically after each write-back (like Typora). When off, use Cmd/Ctrl+S to save.",
          "markdownDescription": "Save the markdown file automatically after each write-back (like Typora). When off, press `Cmd/Ctrl+S` to save."
        },
        "typoraMd.showToolbar": {
          "type": "boolean",
          "default": true,
          "description": "Show the compact formatting toolbar at the top of the WYSIWYG editor."
        },
        "typoraMd.showBlockPanel": {
          "type": "boolean",
          "default": false,
          "description": "Show Vditor's floating block panel (move up/down, delete, set anchor id) that appears when the mouse nears the left gutter of a block.",
          "markdownDescription": "悬停在标题、代码块等**左侧标记**时是否弹出块操作面板（上移 / 下移 / 删除 / 设置锚点 ID）。默认**关闭**（容易误触，且会与块标记、引用竖条叠在一起）。"
        },
        "typoraMd.exitBlockKeys": {
          "type": "string",
          "default": "ctrl+enter",
          "markdownDescription": "在**代码块 / 公式块**内按哪些键**跳出**到块后新建一行（**Typora 用的是 `Cmd/Ctrl + Enter`**）。\n\n多个键用 `,` 分隔，修饰键用 `+` 连接：\n- `ctrl+enter`（默认，macOS 上就是 `Cmd + Enter`，两端通用）\n- `shift+enter`\n- `escape`\n- `cmd+enter`\n- `ctrl+enter,escape`（两个都行）\n- 留空 = 关闭，完全使用 Vditor 原生行为\n\n可用键名如 `enter` / `escape` / `tab`，修饰键 `shift` / `ctrl` / `cmd` / `meta` / `alt`。"
        },
        "typoraMd.exitBlockScope": {
          "type": "string",
          "default": "code,math",
          "markdownDescription": "`typoraMd.exitBlockKeys` 对哪些块生效：\n- `code` —— 代码块（**默认启用**）\n- `math` —— 公式块（**默认启用**）\n- `quote` —— 引用块（默认**不启用**，保留 Vditor 原生行为）\n\n可组合如 `code,math`、`code,math,quote`；留空 = 都不生效。"
        },
        "typoraMd.imageDir": {
          "type": "string",
          "default": "media",
          "description": "Folder (relative to the markdown file) where images pasted into the editor are saved. Use \".\" to save them next to the markdown file.",
          "markdownDescription": "粘贴图片的保存目录（相对当前 Markdown 文件所在目录）。默认 `media`；填 `.` 表示与文档同目录。"
        },
        "typoraMd.tabSize": {
          "type": "number",
          "default": 4,
          "minimum": 0,
          "maximum": 16,
          "description": "Number of spaces inserted when pressing Tab. Default 4; set to 0 to follow VS Code's editor.tabSize.",
          "markdownDescription": "按 `Tab` 插入的空格数。默认 `4`；设为 `0` 表示**跟随 VS Code** 的 `editor.tabSize` 设置。"
        },
        "typoraMd.syncDelayMs": {
          "type": "number",
          "default": 250,
          "minimum": 50,
          "maximum": 2000,
          "description": "Debounce delay (ms) for writing edits back to the markdown file."
        }
      }
    },
    "keybindings": [
      {
        "command": "typoraMd.openWysiwyg",
        "key": "ctrl+shift+t",
        "mac": "cmd+shift+t",
        "when": "editorLangId == markdown && resourceExtname == .md"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -w -p ./",
    "package": "vsce package --no-dependencies",
    "lint": "tsc -p ./ --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.4.0",
    "@vscode/vsce": "^3.2.0"
  }
}